#How to get data into package
#library(ddt)
#usethis::use_data(ddt)
#do not use @export to get data out. It will not be brought into workspace until its name is called.
#copy template from website link put at bottom "ddt" instead of "who"
#when you load dataset it will be ready to be brought into workspace.

# use file path of where

library()
